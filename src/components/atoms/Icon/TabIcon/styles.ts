import {StyleSheet} from 'react-native';

const IconStyleCreater = () => {
  return StyleSheet.create({
    iconStyle: {
      width: 20,
      height: 20,
      tintColor: '#D17842',
      resizeMode: 'contain',
    },
  });
};

export default IconStyleCreater;
